/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["static-00.iconduck.com", "res.cloudinary.com"], // Replace with the domain of your image URL
  },
};

export default nextConfig;
